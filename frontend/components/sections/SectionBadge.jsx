const SectionBadge = ({
    children,
}) => {
    return (
        <span
            className="
                
                text-sm
                font-medium
                text-primary
            "
        >
            {children}
        </span>
    );
}

export default SectionBadge